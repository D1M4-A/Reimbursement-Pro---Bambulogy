name: reimbursement_pro_flutter_web
description: Flutter web host for existing Reimbursement Pro HTML flow.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/ReimbursementPro.html
